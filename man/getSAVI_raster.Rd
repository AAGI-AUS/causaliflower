% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remote_sensing_funcs.r
\name{getSAVI_raster}
\alias{getSAVI_raster}
\title{SAVI raster using Sentinel-2 data}
\usage{
getSAVI_raster(
  bbox,
  start_date,
  end_date,
  pixel_x_size,
  pixel_y_size,
  max_cloud_cover = 50,
  provider = "planetary_computer_v1",
  L = 0.5
)
}
\arguments{
\item{bbox}{A 'bbox' object (WGS84). Create with sf::st_bbox(..., crs = sf::st_crs(4326)).}

\item{start_date}{Start of the date range, "YYYY-MM-DD".}

\item{end_date}{Final date to be observed, "YYYY-MM-DD".}

\item{pixel_x_size}{Longitude resolution in decimal degrees.}

\item{pixel_y_size}{Latitude resolution in decimal degrees.}

\item{max_cloud_cover}{Maximum scene-level cloud cover percentage (0–100). Default 50.}

\item{provider}{Name of the STAC provider to use. Must be a valid entry in
\code{rsi::sentinel2_band_mapping}. Default \code{"planetary_computer_v1"}.}

\item{L}{Soil brightness correction factor (0–1). Default 0.5. Use lower values for high vegetation cover, higher values for low vegetation cover.}
}
\value{
A SpatRaster of median SAVI.
}
\description{
Soil Adjusted Vegetation Index (SAVI) median composite raster. Minimises soil brightness effects in areas with sparse vegetation cover.
}
\examples{
bbox <- sf::st_bbox(c(xmin = 138.712, xmax = 138.718, ymin = -34.902, ymax = -34.906),
                    crs = sf::st_crs(4326))
savi_rast <- getSAVI_raster(bbox, "2023-01-01", "2023-01-10", 0.0001, 0.0001)
terra::plot(savi_rast)
}
\references{
Huete, A. R. (1988). A soil-adjusted vegetation index (SAVI).
Remote Sensing of Environment, 25(3), 295-309.
}
