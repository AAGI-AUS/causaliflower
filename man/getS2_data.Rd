% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remote_sensing_funcs.r
\name{getS2_data}
\alias{getS2_data}
\title{Custom vegetation index time series using Sentinel-2 data}
\usage{
getS2_data(
  bbox,
  start_date,
  end_date,
  pixel_x_size,
  pixel_y_size,
  asset_names,
  index_function,
  max_cloud_cover = 50,
  scl_classes = NULL,
  provider = "planetary_computer_v1"
)
}
\arguments{
\item{bbox}{A 'bbox' object (WGS84). Create with sf::st_bbox(..., crs = sf::st_crs(4326)).}

\item{start_date}{Start of the date range, "YYYY-MM-DD".}

\item{end_date}{Final date to be observed, "YYYY-MM-DD".}

\item{pixel_x_size}{Longitude resolution in decimal degrees.}

\item{pixel_y_size}{Latitude resolution in decimal degrees.}

\item{asset_names}{Character vector of Sentinel-2 band names required by the index (e.g. \code{c("B08", "B04")}).}

\item{index_function}{A function that takes a \code{SpatRaster} and returns a single-layer \code{SpatRaster} of index values. Band layers are accessible by name (e.g. \code{r$B08}).}

\item{max_cloud_cover}{Maximum scene-level cloud cover percentage (0–100). Default 50.}

\item{scl_classes}{Integer vector of SCL (Scene Classification Layer) classes
to mask. Common classes: 9 = cloud high probability, 8 = cloud medium probability,
3 = cloud shadow, 10 = thin cirrus, 1 = saturated/defective.

Set to \code{NULL} (default) to disable pixel-level masking.}

\item{provider}{Name of the STAC provider to use. Must be a valid entry in
\code{rsi::sentinel2_band_mapping}. Default \code{"planetary_computer_v1"}.}
}
\value{
A dataframe with columns for date and value.
}
\description{
getS2_data() calculates a user-defined vegetation index from Sentinel-2
satellite data, returning a time series of per-scene mean values. This
allows computation of any index expressible as a function of Sentinel-2
band assets.
}
\examples{
bbox <- sf::st_bbox(c(xmin = 138.712, xmax = 138.718, ymin = -34.902, ymax = -34.906), crs = sf::st_crs(4326))

# Using s2_index
ndvi_df <- getS2_data(bbox, "2023-01-01", "2023-01-10", 0.0001, 0.0001,
                      asset_names = s2_index$NDVI$assets,
                      index_function = s2_index$NDVI$fun)

# Custom index with cloud masking
evi_df <- getS2_data(bbox, "2023-01-01", "2023-01-10", 0.0001, 0.0001,
                     asset_names = c("B08", "B04", "B02"),
                     index_function = function(r) {
                       2.5 * (r$B08 - r$B04) / (r$B08 + 6 * r$B04 - 7.5 * r$B02 + 1)
                     },
                     scl_classes = c(8, 9, 10))

}
